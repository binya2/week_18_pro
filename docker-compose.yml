version: '3.8'

services:
  # --- Kafka Service ---
  kafka_pro:
    image: confluentinc/cp-kafka:7.8.3
    container_name: kafka
    user: root
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_LISTENERS: INTERNAL://0.0.0.0:29092,EXTERNAL://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka_pro:29092,EXTERNAL://localhost:9092
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka_pro:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_LOG_DIRS: /var/lib/kafka/data
      CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk
    volumes:
      - ./data_vol/kafka_pro:/var/lib/kafka/data
    networks:
      - app-network

  # --- MongoDB Service ---
  mongo_pro:
    image: mongo:latest
    container_name: mongo_pro
    ports:
      - "27017:27017"
    networks:
      - app-network
    volumes: [ "./data_vol/mongo:/data/db" ]

  redis_pro:
    image: redis:latest
    container_name: redis_pro
    ports:
      - "6379:6379"
    networks:
      - app-network
    volumes: [ "./data_vol/redis:/data" ]



networks:
  app-network:
    driver: bridge